 #include "main.h"

/**
 * _abs - finds the absolute value on an int
 * @c: Int whose return value is returned
 *
 * Return: positive value of c
 */
int _abs(int c)
{
	if (c >= 0)
	{
		return (c);
	}
	else
	{
		return (-c);
	}
}
