#include <stdio.h>
/**
 * main - entyr point
 * print base 10 single digit using putchar
 * Return: Return 0
 */
int main(void)
{
	int i = 0;
	while (i >= 0 && i <= 9)
	{
		putchar('i');
		i++;
	}
	putchar('\n');
	return (0);
}
