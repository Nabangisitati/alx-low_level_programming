#include "main.h"

/**
 * mul - multiplies two digits
 * @a: First digit to be multiplied
 * @b: Second digit to be multiplied
 * Return: an integer
 */

int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
