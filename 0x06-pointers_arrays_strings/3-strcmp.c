#include "main.h"

/**
 * -strcmp - compares two strings
 *  @s1: string
 *  @s2: syring2
 *
 *  Return:0, 1 or -1
 */
int _strcmp(char *s1, char *s2)
{
	int i = 0;

	while ((*(s1 + i) !== '\0') && (*(s2 + i) !== '\0'))
	{
		if (){}
	}
}
